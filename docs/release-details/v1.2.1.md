# JLPT MAX Deck v1.2.1 상세 변경 내역

## 릴리스 경계

이번 파일은 정식 `v1.2.1`의 검토 범위와 실제 배포 산출물을 기록합니다. APKG
내부 버전과 다운로드 파일명 모두 `1.2.1`을 사용합니다.

## v1.2.0에서 유지한 것

| 항목 | 결과 |
| --- | ---: |
| 뜻 표시 순서에 맞춰 예문 순서를 바꾼 어휘 | 102개 |
| 슬래시 뜻을 흰색 영역으로 나눠 표시한 어휘 | 651개 |
| 두 변경의 합집합 | 697개 |
| 추가·삭제된 노트 | 0개 |
| 새로 합성한 음성 | 0개 |

102개와 651개에는 두 조건에 모두 해당하는 어휘가 있어 합계가 697개입니다.
뜻 문구 자체와 예문 문장·번역·음성은 이 변경에서 바꾸지 않았습니다.

## 카드 표시

### 한자 버튼

한자 버튼을 직접 만든 클릭 스크립트에서 기본 HTML `details/summary`로
복원했습니다. 그래서 Mac Anki의 WebKit이 처리하는 기본 열기·닫기 동작을
사용합니다. 카드의 사전 버튼과 한자 버튼은 같은 높이와 폭을 쓰도록 맞췄고,
화살표는 닫힘·펼침 모두 같은 11×11px 칸을 사용합니다. 펼칠 때는 같은
화살표를 180도 돌린 뒤 글꼴의 위아래 여백 차이를 3px 보정하므로 `한자`
글자의 가로 위치가 밀리지 않고 두 화살표의 표시 영역도 맞습니다.
한자 패널을 열었을 때 본문이 보이는지도 확인했습니다.

### 후리가나와 고저표

후리가나는 본문 글자의 폭을 차지하는 별도 상자처럼 처리하지 않습니다. 예를
들어 `携帯電話`의 본문과 고저 곡선은 기존 위치를 유지하고, 좁은 모라 칸의
`けい`·`たい` 같은 라벨만 그 칸 안에서 줄여 표시합니다. 긴 후리가나가 옆
단어를 밀어내는 경우에는 실제로 겹칠 때만 겹친 만큼 간격을 넣습니다.

WebKit에서 `<ruby><rt>`를 다루는 방식이 다른 경우에는 후리가나를 카드 전용
표시 상자로 바꿔 같은 위치에 놓습니다. 글꼴 전환 중에는 글꼴이 준비될 때까지
카드를 숨겼다가 한 번에 보여 줍니다. 이 때문에 `財` 카드로 이동할 때 굵은
글꼴이 일반 글꼴로 깜빡이는 현상과 고저표의 초기 위치 튐을 함께 줄였습니다.

## 콘텐츠 교정

| 제보 | 수정 |
| --- | --- |
| `猛` 선택지 후리가나 | `たけし` → `もう` |
| `縁` 뜻 간격 | `① 인연 연  ② 가장자리 연·테두리 연` → `① 인연 연 ② 가장자리 연 · 테두리 연` |
| `木曜日` 문제 해설 | 정답 ②의 해설이 보기 ②를 설명하도록 순서 교정 |

이 세 교정은 기존 노트·카드 ID를 바꾸지 않는 카드 생성 단계의 보정입니다.

## 익명 사용 통계

공유를 꺼도 기존 익명 설치 식별자는 기기 저장소에 남습니다. 공유를 다시 켜거나
덱을 업데이트하고 앱을 껐다 켜도 같은 설치로 집계됩니다. 앱 데이터 삭제나
재설치 때만 새 식별자가 만들어집니다.

## 제보 테스트 덱 전수 목록

이번 릴리스에 포함한 제보를 모두 작은 테스트 덱으로 분리해 9노트·14카드·22개
미디어로 확인했습니다.

| 제보 ID | 카드 | 확인 내용 |
| --- | --- | --- |
| R-76BD4984E3B1 | `援助` | 후리가나가 본문을 밀지 않고 겹칠 때만 간격 추가 |
| R-B701504DE200 | 긴 후리가나 | 본문 폭을 늘리지 않음 |
| R-39D2EB0E85BA | 글꼴 전환 | 전환 중 카드 깜빡임·높이 튐 없음 |
| R-A354ED4ED268 | `携帯電話` | 좁은 고저표 모라 라벨만 조정 |
| R-FB369F0265FD | `暴れる` | 한자 상세 패널 열기·닫기, 화살표 고정 칸·글자 위치 |
| R-AEC03A03B197 | `財` | 글꼴 전환 뒤 표시 안정성 |
| R-10906930E034 | `縁` | 뜻이 `가장자리 연 · 테두리 연`으로 붙어 보임 |
| R-A3614346B234 | `猛` | 선택지 후리가나가 `もう`인지 확인 |
| R-9F1D1178E957 | `木曜日` | 보기와 해설 번호 매핑 일치 |

테스트 덱은 배포용 덱에 포함하지 않고, 이번 패치의 재현 범위만 확인하는
검사용 산출물로 유지합니다.

## 전체 APKG와 업데이트 확인

- private 전체 APKG: 16,240노트·22,402카드
- public 기본 APKG: 13,903노트·20,065카드
- 음성: 18,435개 기존 바이트 재사용, 새 합성 0개
- 한자 builder: 같은 검증된 입력으로 v1.2.1 파일 재생성
- v1.2.0 컬렉션에 가져온 뒤 새 노트 0개·중복 0개·충돌 0개
- 기존 GUID·학습 일정·노트 유형과 카드 구조 유지
- 릴리스 워크플로 테스트와 pyright 통과

## 산출물

| 파일 | bytes | SHA-256 |
| --- | ---: | --- |
| `JLPT-MAX-Deck-1.2.1.apkg` | 901,932,089 | `ab7e97f1677c0e182c6c3dca19d19607816479c1ff0e2c0bfd42e517213d3ec2` |
| `JLPT-MAX-kanji-builder-1.2.1.zip` | 840,514 | `f50838a6d24afa902eef7a0cfce81d4da339e5e50fe70fbf142dd9060d9fb795` |
| `SHA256SUMS` | 190 | `e2022c576a4f113ac0e126f441b480706685de3737033e3e2efd2ac3f1a96daf` |

전체 private APKG SHA-256은
`409bd3139bedc5085a44e07179333c031b84bc76472cd8cf12717b115a7c7ffc`입니다.
